Please run the Server-side-code using intellij
and the client-side-code must be run after installing the libraries react-router-dom, axios. 
Also the user needs to have MySQL database would have to change the JDBC url before running backend code. 